aim-> to intialize a git repo and basic distributed system learning

commands used ->

git init -> initialize a git repo (converting normal directory to git -	ss) 

git commit -> snap shot of changes 

git status -> display current state of repo

git log -> used to view commit history


