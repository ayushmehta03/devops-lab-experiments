#Types of merging 

fast forward (ff) and non fast forward(nff)

fast forward merge -> Git simply moves the pointer of the target branch (e.g., main) forward to the latest commit of the source branch

non fast forward merge -> Git creates a new "merge commit" that ties the two branches together, even if a fast-forward is possible


